<h1>All Pets for <%= @owner.first_name %></h1>
<ul><% @owner.pets.each do |pet| %>
  <li> <%= link_to pet.name, owner_pet_path(@owner, pet) %></li>
  <% end %>
</ul>
<hr>

<%= link_to "add new pet", new_owner_pet_path %>
